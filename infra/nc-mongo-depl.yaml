apiVersion: apps/v1
kind: Deployment
metadata:
  name: nc-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nc-mongo
  template:
    metadata:
      labels:
        app: nc-mongo
    spec:
      containers:
        - name: nc-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: nc-mongo-srv
spec:
  selector:
    app: nc-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
